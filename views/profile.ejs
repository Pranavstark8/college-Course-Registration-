<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Body Styling */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #e8f0f2, #ffffff);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #34495e;
        }

        /* ID Card Container */
        .container {
            background: linear-gradient(135deg, #ffffff, #f7fafc);
            width: 360px;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            border: 1px solid #e1e8ed;
            position: relative;
        }

        /* Profile Icon Circle */
        .icon {
            font-size: 3.5rem;
            color: #3498db;
            background: linear-gradient(135deg, #ebf5fb, #d6eaf8);
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        }

        /* Header Styling */
        h2 {
            font-size: 1.8rem;
            color: #2c3e50;
            font-weight: 600;
            margin-bottom: 8px;
        }

        /* Subheader Styling */
        h3 {
            font-size: 1.3rem;
            color: #2980b9;
            font-weight: 500;
            margin-top: 20px;
            margin-bottom: 8px;
            border-bottom: 1px solid #d6eaf8;
            padding-bottom: 5px;
            display: inline-block;
        }

        /* Details Styling */
        p {
            font-size: 1rem;
            color: #5d6d7e;
            margin: 6px 0;
        }

        /* Highlight for Important Text */
        .highlight {
            font-size: 1.05rem;
            color: #34495e;
            font-weight: 600;
            padding: 4px 8px;
            background-color: #eaf2f8;
            border-radius: 8px;
            display: inline-block;
            margin-top: 5px;
        }

        /* Divider Line */
        .divider {
            width: 100%;
            height: 1px;
            background-color: #e1e8ed;
            margin: 15px 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Profile Icon -->
        <div class="icon">👤</div>

        <!-- Profile Details -->
        <h2>
            <%= user.name %>
        </h2>
        <p class="highlight">
            <%= user.username %>
        </p>
        <p class="highlight">Role: <%= user.role %>
        </p>

        <!-- Divider Line -->
        <div class="divider"></div>

        <!-- Role-Specific Information -->
        <% if (user.role==='student' && roleData) { %>
            <h3>Student Profile</h3>
            <p>Semester: <span class="highlight">
                    <%= roleData.semester %>
                </span></p>
            <p>Start Year: <span class="highlight">
                    <%= roleData.startYear %>
                </span></p>
            <p>End Year: <span class="highlight">
                    <%= roleData.endYear %>
                </span></p>
            <% } else if (user.role==='teacher' && roleData) { %>
                <h3>Teacher Profile</h3>
                <p>Department: <span class="highlight">
                        <%= roleData.department %>
                    </span></p>
                <% } else if (user.role==='admin' ) { %>
                    <h3>Admin Profile</h3>
                    <p>Admins do not have specific data beyond their basic profile information.</p>
                    <% } %>
    </div>
</body>

</html>