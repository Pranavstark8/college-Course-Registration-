<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Background with Animation */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            background: radial-gradient(circle at top, #f5f7fa, #c3cfe2);
            overflow: hidden;
            animation: gradientAnimation 10s ease infinite;
        }

        @keyframes gradientAnimation {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        /* Container Styling */
        .container {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 20px;
            max-width: 600px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            text-align: center;
            transition: transform 0.3s ease;
            animation: slideIn 1s ease;
        }

        .container:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        }

        /* Keyframes for Slide-In Animation */
        @keyframes slideIn {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Header and Text Styling */
        h2,
        h3 {
            color: #34495e;
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            opacity: 0.9;
            text-transform: capitalize;
        }

        h3 {
            font-size: 1.8rem;
            color: #3498db;
            margin-top: 30px;
        }

        p {
            font-size: 1.2rem;
            color: #7f8c8d;
            transition: color 0.3s ease;
        }

        /* Profile Icon */
        .icon {
            font-size: 4rem;
            color: #3498db;
            margin-bottom: 20px;
            animation: bounce 1.5s infinite;
        }

        @keyframes bounce {

            0%,
            20%,
            50%,
            80%,
            100% {
                transform: translateY(0);
            }

            40% {
                transform: translateY(-15px);
            }

            60% {
                transform: translateY(-7px);
            }
        }

        /* Hover Effect for Paragraph */
        p:hover {
            color: #34495e;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Animated Profile Icon -->
        <div class="icon">👤</div>

        <!-- Profile Details -->
        <h2>Profile of <%= user.name %>
        </h2>
        <p>Email: <%= user.username %>
        </p>
        <p>Role: <%= user.role %>
        </p>

        <!-- Role-Specific Information -->
        <% if (user.role==='student' && roleData) { %>
            <h3>Student Profile</h3>
            <p>Semester: <%= roleData.semester %>
            </p>
            <p>Start Year: <%= roleData.startYear %>
            </p>
            <p>End Year: <%= roleData.endYear %>
            </p>
            <% } else if (user.role==='teacher' && roleData) { %>
                <h3>Teacher Profile</h3>
                <p>Department: <%= roleData.department %>
                </p>
                <% } else if (user.role==='admin' ) { %>
                    <h3>Admin Profile</h3>
                    <p>Admins do not have specific data beyond their basic profile information.</p>
                    <% } %>
    </div>
</body>

</html>